# Completion Phase 7 Task 7.2 — land and verify

- **Base inspected:** `e59ad7b`; GitHub `main` at `4f4f431` (PR #258);
  local branch 351 commits ahead; open PRs #279, #280, #282–#288, #290, #291.
- **Dependencies:** none for verification; Task 7.1 adds commits the delivery
  sequence must include.
- **Fixes:** `docs/COMPLETION_PLAN.md` O12; records O14.
- **Executor:** the delivery split is owned by the other session (handoff,
  "Hosted delivery"). This brief is the coordinator's verification concern:
  Opus 5 `medium` for PR bodies and split proofs when this session authors
  one; Opus 5 `low` for the record.

## Findings at the inspected base

`docs/CI_DELIVERY_SPLIT_PLAN.md` names fourteen commits over the 800-line
ceiling and a dependency-ordered sequence; it is a map, not a status. The
handoff's Phase 6 engineering checkpoint records `PR_BASE=eebb132… make
check-size` at 75,566 lines and says hosted checks were never verified for a
Phase 5–6 candidate. The application-wide audit lists the unlanded branch as
a release blocker. Standing authorizations: push and stacked PRs to
`EricMG13/caos-v2`; over-cap merge only for a proven-indivisible commit with
the split attempt recorded in the PR body (precedents #247, #271, `9bf20b2`).
The audit remediation stream lands its own commits through the same
sequence; its waves are recorded here as they integrate.

## Contract and scope

1. A table in the handoff under "Completion Phase 7 — delivery record": one
   row per PR — number, head, base, counted size (`make check-size
   PR_BASE=<base>` on the exact head), the nine required checks as GitHub
   reports them (`gh pr checks <n>`, read-only), merged commit or open.
   Never a local run standing in for a hosted status.
2. For an over-cap merge: the split attempt (halves tried, each measured, why
   each fails) in the PR body, referenced from the row.
3. A second table, "Remediation stream": wave, tasks, integration commit,
   full-gate result as that stream reports it, and which Phase 10 and
   Phase 12 dependencies (T7, T8, T11, T2, T3, D2) are thereby met.
4. When the branch is fully landed: the `main` commit, a GitNexus refresh at
   it, the handoff's checkpoint table updated; until then the row says which
   phase's last PR has merged.
5. This session does not force-push, alter rulesets, request bypass or merge
   with a required check red other than `size` under the standing exception.
   If the `main gates` ruleset is enforced, ask the owner to merge.

## Acceptance checks

- Every merged PR since #258 has a row with hosted results read from GitHub.
- No row's size was measured on anything but the PR's exact head and base.
- `CI_DELIVERY_SPLIT_PLAN.md` gains a "landed as" column filled for every one
  of its fourteen commits or says which are pending.
- The remediation table names the landed waves and the dependencies met.

## Concern boundaries and gates

Docs-only from this workbench; no code. Runs continuously through Phase 7 and
closes when the last of Task 7.1's commits is on `main`. Provider variables
removed from every command; `gh` reads only.
