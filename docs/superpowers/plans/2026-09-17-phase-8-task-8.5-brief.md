# Completion Phase 8 Task 8.5 — vendor change requests

- **Base inspected:** `e59ad7b`. Docs only.
- **Dependencies:** none. Tasks 9.5–9.7 and the bundle-side ledger entries
  wait on the answers.
- **Fixes:** `docs/COMPLETION_PLAN.md` O03 and O11 (requests, not changes).
- **Executor:** Fable 5.1 `high` (governance text). No review beyond the
  owner's reading. No vendor file changes.

## Findings at the inspected base

The vendored tree is upstream `EricMG13/Deploy-V@c4d2e356` plus three
authorized edits (§61, §63). Five things the programme needs are the
bundle's to change:

1. **LITE producers.** `CP_DEPLOY_V_EXECUTION_PROFILES_v1.json`
   `retained_lite_capabilities` says CP-2A and CP-2H accept
   `lite_fundamental_credit_screen` and `lite_liquidity_sensitivity_screen`,
   CP-3C those two plus `lite_market_recovery_opportunity_screen` and
   `lite_legal_structure_capacity_screen`, CP-4C the last; the bundle ships
   their payload schemas (`CP-OS_MIRROR_CP-L20…`, `L23`, `L30`, `L40`) and
   lists CP-L20/L23/L30/L40 in the LITE membership, but ships no skill for
   any of them and no catalog edge carries the objects. Three LITE pathways
   cannot be enabled (ledger: "a vendor owner for every accepted object
   before those routes are enabled").
2. **The disqualifier marker split** (§66): `full_run_disqualifiers` conflates
   fixture markers with `SOURCE_LIMITED_NOT_COMMITTEE_READY`; enforcing the
   list refuses seven of 25 retained honest handoffs.
3. **CP-0 gating versus classification**: `SKILL.md` §331/§349/§357 and
   `CANON_SHARED.md` §632 make CP-0 gate per consumer; the owner's stated
   intent (16 September) is classification only.
4. **Three unshipped rules**: `semantic_rules`, `document_substrings_casefold`
   and the LITE `required_payload_fields` are declared without code.
5. **LITE scope-to-status mapping**: `decision_scope: SCREENING_ONLY` maps to
   no `committee_status`, so a LITE handoff saying `Committee Ready` validates.

## Contract and scope

One document per item under `docs/requests/`, each with: the exact files and
lines in the vendored bundle; the evidence in this tree (run ids, refused
bodies, ledger entries, decision sections); what the change unblocks (the
pathways, the ledger entries, the qualification keys); the two ways it can
land — an upstream build pulled with `verify_package.py --refresh` and a
§13-style pin move, or an authorized in-tree edit under §61's precedent with
its own decision entry; and what the host does meanwhile (holds the pathway;
projects the flag; enforces nothing). The LITE-producers request also names
the alternative the profile allows — an edge carrying `accepted_object_id`
so an existing owner's object is offered — and says why the host cannot take
it alone.

## Acceptance checks

- Five files exist; each names its blocked pathways or ledger entries; the
  handoff records each as pending with a date.
- `git diff --stat vendor/` is empty.

## Concern boundaries and gates

One docs commit. Provider variables removed.
