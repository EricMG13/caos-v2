# Completion Phase 8 Task 8.1 — register keys

- **Base inspected:** `e59ad7b`; rebase onto Phase 7's accepted commit.
  Isolated branch `codex/phase8-8-1`; UUID-owned test database on 55437.
- **Dependencies:** none. Task 8.3 and every pathway task consume it.
- **Fixes:** `docs/COMPLETION_PLAN.md` O08; the ledger's "An answer key names
  citations, not figures" and "A citation key measures neither the conclusion
  nor its soundness".
- **Executor:** Opus 5 `medium`. Ordinary exact-range review on Opus 5
  `medium`. One targeted prompt on Opus 5 `max` with `ultrathink` (below).

## Findings at the inspected base

- `server/qualification/matrix.py:146` `QualificationCase(label, documents,
  profile_id, selection_id, expects: tuple[ExpectedCitation, ...], subject,
  forecast: ExpectedForecast | None, expected_refusal, expects_ready:
  tuple[str, ...], expects_projection: tuple[ExpectedProjection, ...],
  model_extension: bool)`; `MatrixRow(case_label, proven, refusal, met,
  missed, forecast_met, expected_refusal_met, ready_met, projections_met)`.
- `ExpectedProjection(module_id, field, value)` over `PROJECTION_FIELDS`
  (`:132`), scored by `_projections_met` (`:483`) through
  `accepted_projections` (rebuilds identity, requires the record to bind the
  Markdown, re-parses the projections). A module the case names that
  produced no artifact is a miss.
- `server/qualification/on_disk.py:265` `_projections(item)` reads
  `expects_projection` rows with exactly `_PROJECTION_KEYS`, refuses an
  unknown field at load, bounds strings with `_bounded` (`BoundaryText`,
  128), refuses duplicates; `:74-87` lists the declared manifest keys.
- `qualification_set_digest` (`:223`) digests every case and key,
  order-independent.
- The vendor ships the reader: `completeness_check.find_registers(handoff_text,
  register_ids=None) -> {register_id: (header, [rows])}`
  (`vendor/deploy-v/skills/cp-os-credit-os/scripts/completeness_check.py:217`),
  locating a register by its id in a heading or caption within the few
  lines above a pipe table; loaded as `VendorContract.completeness_check`
  (`server/methodology/vendor.py:59`).
- The accepted Markdown is read by `accepted_handoff` (the reader
  `_forecast_met` already uses); re-verify its exact return at the base.

## Contract and scope

1. `ExpectedRegister(module_id: str, register_id: str, row_key:
   tuple[tuple[str, str], ...], column: str, expected: str)` in `matrix.py`;
   `QualificationCase.expects_register: tuple[ExpectedRegister, ...] = ()`;
   `MatrixRow.registers_met: bool | None = None`; `_registers_met` scored
   beside `_projections_met` from the same accepted rows: `find_registers`
   on the verified Markdown for the key's `register_id`; the row is the one
   whose cells under every `row_key` column equal the key's values
   (normalised); exactly one such row or the key is a miss; the `column`
   cell compared after NFC normalisation and whitespace collapse, case
   preserved; a module with no accepted artifact is a miss.
2. `on_disk.py`: `expects_register` in the declared keys; `_registers(item)`
   mirroring `_projections`; each row `{module_id, register_id, row_key:
   {column: value, ...}, column, expected}`, every string `_bounded`;
   duplicates refused; `assert_measurable` refuses a key whose `row_key` is
   empty.
3. `assert_unambiguous` cannot see the run's table; ambiguity is a scoring
   miss with the reason recorded on the row (`MatrixRow.register_notes:
   tuple[str, ...]`, host words only, never cell text).
4. The digest covers the new keys (`_digested`).
5. `qualification/vmo2-fy2025/qualification.json` gains one register key per
   module authored from the two releases — CP-L10 `TL10.2` on the Q4
   goodwill impairment topic; CP-0 `T8` on `CP-L10` readiness; CP-5 `T5B.5`
   on the covenant-leverage claim's `Status` — and the borrowing-capacity
   citation key re-cast to the line that carries the fact (the ledger names
   the current key as the subordinate clause).

## RED-first acceptance checks

- `tests/test_qualification_matrix.py::test_a_register_key_fails_a_run_whose_cell_says_the_wrong_thing`
  — a deterministic LITE run whose CP-L10 TL10.2 row concludes differently
  from the key; `projections_met` True, `registers_met` False.
- `test_a_register_key_with_an_ambiguous_row_key_is_a_miss_not_a_match`.
- `test_a_register_key_matches_after_nfc_and_whitespace_normalisation_only`
  (case differs → miss).
- `tests/test_qualification_on_disk.py::test_expects_register_loads_and_moves_the_digest`
  and `test_an_empty_row_key_refuses_at_load`.
- `test_the_vmo2_set_still_loads_with_its_recast_citation_key`.

**Targeted prompt (Opus 5 `max`):** `ultrathink: Construct a handoff that
meets an ExpectedRegister key while concluding the opposite of what the key
was authored to check — through row order, a duplicated register heading, a
fenced table, a merged cell, or a cell that matches after normalisation.
Name the parser rule that closes each and which is the vendor's to close.`

## Concern boundaries and gates

Two concerns: the key form and scoring; the VMO2 set's keys (fixture-only,
no review). Serial backend gate per commit; provider variables removed. No
provider call: the VMO2 keys are authored from the documents and are not
scored against a live run here.
