# Completion Phase 9 Task 9.1 — `LITE_PORTFOLIO_DECISION` (CP-0 → CP-L10)

- **Base inspected:** `e59ad7b`; rebase onto Phase 8's accepted commit.
  Isolated branch `codex/phase9-9-1`; UUID-owned test database on 55437.
- **Dependencies:** Task 8.1 (register keys) for the set; remediation T7
  landed before the live run.
- **Fixes:** `docs/COMPLETION_PLAN.md` O02 (one pathway); the ledger line
  "`ADAPTER_ROUTES`: LITE portfolio decision, CP-0 -> CP-L10, has no
  contract test" (`CLAUDE.md:451`).
- **Executor (template rows):** no unproven module, so step 1 is a read of
  CP-L10's `SKILL.md` under this selection only (Opus 5 `medium`); steps 2–4
  Opus 5 `medium`; step 5 Opus 5 `low`; step 6 Opus 5 `medium` from the
  documents, figures confirmed by the owner; step 7 the coordinator under the
  owner's authorization.

## Findings at the inspected base

- Catalog: `LITE_CREDIT_22/LITE_PORTFOLIO_DECISION` nodes CP-0, CP-L10; one
  REQUIRED edge CP-0 → CP-L10; `decision_scope: SCREENING_ONLY`.
- Both modules are in `ADAPTER_MODULES`; the pair is not in `ADAPTER_ROUTES`
  (`server/methodology/handoff.py:55`), so `create_run` refuses
  `ROUTE_NOT_ENABLED` and execution `HANDOFF_MODULE_UNSUPPORTED`.
- `tests/lite_route_fixtures.py::realistic_handoff_markdown(ident, knobs)`
  builds vendor-valid CP-0 and CP-L10 handoffs from `load_contract`;
  `tests/test_canonical_runtime.py::test_a_lite_route_completes_through_the_real_runtime`
  is the whole-route shape; `tests/test_relative_value_route.py::DISABLED`
  enumerates FULL pathways only and `tests/test_disabled_routes.py` proves a
  disabled route pins and governs but makes no attempt.
- CP-L10's owned object `lite_financial_change_screen` has no consumer on
  this route; `named_objects` holds nothing.
- Corpus in hand for two sets: VMO2 (Q3 and Q4 releases) and CCL (10-K text).

## Contract and scope

1. `tests/test_lite_portfolio_route.py`: `test_lite_portfolio_route_completes_proves_and_freezes`
   (two nodes, deterministic provider, proof, canonical deliverable frozen);
   `test_lite_portfolio_requests_fit_the_request_ceiling`;
   `test_a_blocked_cp0_verdict_holds_cp_l10_and_calls_nothing_after`;
   `test_a_validated_blocked_cp_l10_ends_the_run_blocked` (the route's
   terminal is CP-L10, whose `Blocked` ends the run with `blocked_by`);
   `test_lite_portfolio_is_the_only_newly_enabled_route` mirroring the
   RELATIVE_VALUE guard so `ADAPTER_ROUTES` is asserted exactly.
2. `ADAPTER_ROUTES` gains `("LITE_CREDIT_22", "LITE_PORTFOLIO_DECISION")`;
   `DISABLED` in `test_relative_value_route.py` generalised to "every
   pathway not in `ADAPTER_ROUTES`" over both profiles; the run document's
   route choices (`reads/run.py:175`) show it.
3. Sets `qualification/vmo2-fy2025-portfolio/` and
   `qualification/ccl-fy2025-portfolio/`, each `qualification.json` naming
   the already-admitted documents by path and this selection; keys:
   `expects_ready: ["CP-L10"]`; `expects_projection` `CP-L10 decision_scope
   SCREENING_ONLY` and `qa_status Restricted`; `expects_register` on
   `TL10.2` — VMO2: the topic row whose `topic_label` is the Q4 goodwill
   impairment with `scope_status` from the release; CCL: the
   debt-extinguishment topic (the answer key's three-figure trap: 409 P&L,
   401 add-back, 272 cash) — and on `TL10.1` the source row for each
   document; `expects` one citation each naming the fact-carrying line.
4. The ledger line struck in the enablement commit.
5. Step 7 when authorized: `scripts/qualify.py qualification/vmo2-fy2025-portfolio
   --expect-identity <profile> --ceiling <n>` and the same for CCL; captures
   committed; verdicts signed or reasons recorded.

## RED-first acceptance checks

The five tests above fail at the base (`HANDOFF_MODULE_UNSUPPORTED` before
any attempt) and pass after; `tests/test_disabled_routes.py` still proves
every other LITE pathway refuses; `tests/test_qualification_on_disk.py` loads
both sets and their digests are recorded in each `RESULT.md`.

## Concern boundaries and gates

Two concerns: tests and enablement (one commit, touches `handoff.py`'s
constant line only — rebase onto the remediation wave-3 integration if it has
not landed); the two sets (fixture-only). Serial backend gate; no provider
call except the authorized step 7.
