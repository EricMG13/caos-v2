# Completion Phase 7 Task 7.1 — make the record true

- **Base inspected:** `e59ad7b` on `codex/execute-repair-plan` (Phase 6 signed
  off, `docs/DECISIONS.md` §69). Isolated branch `codex/phase7-7-1`.
- **Dependencies:** none. Runs first; Task 7.2 records beside it.
- **Fixes:** `docs/COMPLETION_PLAN.md` O13; records O14. No code path
  changes; one new gate script and its test.
- **Executor:** Opus 5 `low` for the regeneration and relabelling; the new
  test and script at Opus 5 `medium`. No ordinary review (docs and a
  test-only gate). Whole-phase reviews stay coordinator-owned on Fable 5.1
  `max`.

## Findings at the inspected base

`CLAUDE.md`'s known-gaps ledger has 87 open bullets and 35 struck. Three open
entries describe states later work closed: "A proof is held and not stored"
(migration `0020_qualification_performed`; `perform()` persists the snapshot),
"A verdict is read and not stored" (migrations `0018`, `0019`,
`record_verdict`), and the Phase 10 wording that `qualification_verdicts`
cannot be written (§65's route). Two Phase 3 entries carry upgrade paths this
plan withdraws: "A route's predicates are frozen and never evaluated" (the
catalog carries no CONDITIONAL edge; O16 is a guard) and "BLOCKED ends the
run; recovery is a new run" (its "governed resume" would reopen a run whose
pins cannot change; O17 is a successor link). The four headings "Phase 7",
"Phase 8", "Phase 9", "Phase 10" are rebuild labels.

`docs/CLAUDE_CODE_HANDOFF.md`'s checkpoint table says the next task is the
§62 CP-5 deferral, which §63 resolved and §69 signed; the launch text still
points at `PHASE_3_ONWARDS_GOAL_PROMPT.md`; nothing records the audit
remediation stream (`sdd/t1`–`sdd/t6` at `/private/tmp/caos-sdd-t{1..6}`;
`sdd/t1` at `4103121`, `sdd/t6` at `65f9d10`).

`docs/feature-status.csv` has six "Gap documented" rows; two say "health
route not built" (served since §53.8 at `server/api/health.py:218`), three
say "the section API is feature work" (served since §50), one is unverified.

`gemini-audit.md`, `PATHFINDER-2026-09-15/` and `AGENTS.md` are untracked;
the concurrent review `docs/reviews/2026-09-17-gemini-audit-adversarial-review.md`
carries the verdict on every claim the first two make.

## Contract and scope

1. Add `scripts/ledger_state.py`: reads `CLAUDE.md`, yields each top-level
   ledger bullet as `(struck: bool, heading: str, test_names: tuple[str, ...])`
   where test names are every `test_[a-z0-9_]+` token in the bullet. Pure
   stdlib; `--report` prints a table.
2. Add `tests/test_ledger.py`:
   - `test_every_struck_ledger_entry_cites_a_test_the_suite_defines` — every
     test name in a struck entry is defined under `tests/` or
     `frontend/tests/` (reuse `tests/test_phase_exits.py`'s
     `defined_in_the_workspace_suite` reader).
   - `test_no_open_ledger_entry_cites_a_test_that_proves_its_closure` — an
     open entry containing "*Upgrade:*" that names an existing test whose
     name contains the entry's key phrase is flagged; the three entries above
     are the RED.
   - `test_the_ledger_scanned_something` — more than 80 bullets read.
3. Strike the three closed entries with `~~` and the closing commit; rewrite
   the two Phase 3 entries' *Upgrade* lines to O16 and O17; relabel the four
   rebuild headings "Rebuild Phase N (historical)"; add "**Completion Phase
   7.**" at the top of the ledger with one entry: the remediation stream's
   findings are tracked in its review and plan, not here.
4. Rewrite the handoff's checkpoint table: latest accepted phase Phase 6 at
   `e59ad7b` (§69); next task Completion Phase 7 Task 7.1; launch text
   `PHASE_7_ONWARDS_GOAL_PROMPT.md`; a "Completion plan" row linking
   `docs/COMPLETION_PLAN.md`; a "Remediation stream" row with the worktrees,
   branches and commits observed. Leave every acceptance record below intact.
5. Regenerate the six `feature-status.csv` rows: `Post-fix result` names the
   test that now proves the row; `Status` becomes `Passing`. No other row.
6. Move `gemini-audit.md` and `PATHFINDER-2026-09-15/` under
   `docs/reviews/supplemental/` with a two-line header naming the concurrent
   review, or delete them (owner's choice; default move). `AGENTS.md` is the
   GitNexus-generated instruction file: commit it or add it to `.gitignore`
   with one line saying why.
7. Do not edit `docs/REPAIR_PLAN.md`, `docs/DECISIONS.md` or any vendor file.

## RED-first acceptance checks

- `test_no_open_ledger_entry_cites_a_test_that_proves_its_closure` fails at
  the base on the three entries and passes after the strike.
- `test_every_struck_ledger_entry_cites_a_test_the_suite_defines` passes at
  the base and after (a guard).
- `test_the_ledger_scanned_something` fails on an empty file.
- `tests/test_gate_scripts.py` gains one case driving `ledger_state.py
  --report` the way CI drives the other scripts.
- `git status --short` shows nothing untracked outside `.claude/`.

## Concern boundaries and gates

One commit for the script and test, one for the ledger and handoff, one for
the CSV and the moved files. Docs are excluded from the size gate; the
script and test are counted. Commit checks: lint, types, `check_tested`,
`tests/test_ledger.py`, `tests/test_gate_scripts.py`,
`tests/test_phase_exits.py`. Provider variables removed from every command.
