# Completion Phase 10 Task 10.2 — the conditional-edge guard

- **Base inspected:** `e59ad7b`; rebase onto Phase 8's accepted commit.
  Isolated branch `codex/phase10-10-2`.
- **Dependencies:** none.
- **Fixes:** `docs/COMPLETION_PLAN.md` O16; rewrites the ledger's "A route's
  predicates are frozen and never evaluated".
- **Executor:** Opus 5 `low`. No ordinary review (one refusal and two
  tests). No targeted prompt.

## Findings at the inspected base

`server/engine/route.py:43` declares `EdgeType.CONDITIONAL` and `:59` puts
it in `BLOCKING`; `_edges_among` (`:511`) accepts any `EdgeType` the profile
names. `ResolvedRoute.predicates` (`:165`) carries only
`host_manifest_sha256` and is consulted by nothing. Counted at
`vendor/deploy-v/skills/cp-os-credit-os/references/CREDIT_OS_V_MODULE_CATALOG_v2.json`:
60 REQUIRED, 26 OPTIONAL, 29 ADVISORY, 1 QA_GATE, 0 CONDITIONAL typed edges.
`tests/test_route_resolution.py::test_a_conditional_verdict_blocks_like_a_blocked_one`
covers the CP-0 *verdict*, not an edge. An upstream build that introduced a
CONDITIONAL edge would pin a route whose target blocks whatever the evidence
says, with nothing in the suite saying so.

## Contract and scope

1. `server/refusals.py`: `ROUTE_EDGE_UNSUPPORTED`, with its `clears` text
   "the engine evaluates this edge type" in the refusal table.
2. `_edges_among` refuses `ROUTE_EDGE_UNSUPPORTED` for
   `EdgeType.CONDITIONAL` before constructing the `Edge`. `BLOCKING` keeps
   the member (the type still exists in the bundle's vocabulary, CONTEXT.md).
3. `tests/test_bundle_pin.py::test_the_vendored_catalog_carries_no_edge_this_engine_cannot_evaluate`:
   loads the pinned catalog through the same reader `resolve_route` uses,
   counts typed edges per type, asserts `CONDITIONAL == 0` and that the four
   other counts equal the pinned tuple `(60, 26, 29, 1)` — so a bundle pull
   that moves any of them is visible, not only the one that matters.
4. `tests/test_route_resolution.py::test_a_profile_with_a_conditional_edge_is_refused_at_resolution`:
   a hand-built profile with one CONDITIONAL edge refuses at `resolve_route`
   with the new code; nothing is pinned.
5. `CLAUDE.md` Phase 3 ledger entry rewritten: the branch is unreachable on
   the vendored catalog; the evaluator is owed the day the guard fails,
   which is also the day a grammar has a real predicate to parse.

## RED-first acceptance checks

- The resolution test fails at the base (the route pins) and passes after.
- The catalog test passes at the base and after (it is a guard); a copy of
  the catalog with one edge retyped to CONDITIONAL fails it (asserted in the
  same test via a mutated in-memory copy).
- `tests/test_route_resolution.py` and `tests/test_bundle_pin.py` green;
  `check_vocabulary` clean (`CONDITIONAL` remains the bundle's word).

## Concern boundaries and gates

One commit, well under the size gate. Lint, types, `check_tested`, the two
test files. No vendor edit. Provider variables removed from every command.
