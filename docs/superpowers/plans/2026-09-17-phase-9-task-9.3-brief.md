# Completion Phase 9 Task 9.3 — `LITE_DECISION_LEDGER` (CP-0 → CP-8)

- **Base inspected:** `e59ad7b`; rebase onto Phase 8's accepted commit.
  Isolated branch `codex/phase9-9-3`; UUID-owned test database on 55437.
- **Dependencies:** Task 8.1; the decision record from Task 8.4's sourcing
  list; remediation T7 before the live run.
- **Fixes:** `docs/COMPLETION_PLAN.md` O01 (CP-8), O02 (one pathway), O05.
- **Executor (template rows):** step 1 Opus 5 `max` `ultrathink` on CP-8's
  registers; steps 2–4 Opus 5 `medium`; step 5 Opus 5 `low` (the owner
  supplies the memo); step 6 Opus 5 `medium`; step 7 under authorization.

## Findings at the inspected base

- Catalog: nodes CP-0, CP-8; one REQUIRED edge; `SCREENING_ONLY`. CP-8 is
  `PROFILE_AGNOSTIC` in the LITE profile (no named object).
- `vendor/deploy-v/skills/cp-8-decision-ledger-post-mortem/SKILL.md`:
  dependencies CP-0 only; registers T7.1 (Action Bias; Posture; Instrument;
  Size; Decision Date; Decision-Maker Role; Module Status), T7.2 (Thesis;
  Greatest Uncertainty; Flagged Catalysts/Downside Paths; Expected
  Spread/Return; Expected Rating Path; Expected Hold; Exit Triggers; Evidence
  ID), T7.3 (Metric; Realized Value; As-of Date; Source; Interim/Final;
  Evidence ID), T7.4 (Metric; Expected; Realized; Variance; Confidence;
  Evidence ID), T7.5 (Variance; Attribution Label; Process Sound?;
  Originating Module; Basis; Evidence ID), T7.6 (Pattern; Target Module;
  Specific Prior; Supporting Decisions; Advisory Recommendation), T7.7
  (aggregate: hit rate, realised-vs-expected by cut, attribution
  distribution, lessons), T7.8 (Gap; Missing Item; Why It Matters; Impact;
  Follow-Up); every column critical. Reader question: how did the outcome
  compare with the recorded thesis, why, and what lesson follows.
- The decision record CP-8 post-mortems is therefore a *document* in the
  pack: what was decided, when, on what thesis and expectations — and the
  outcome period is a later filing. Nothing in this system has filed a
  deliverable that could serve as T0 yet (Phase 12); an owner-supplied memo
  is the T0 record until then.

## Contract and scope

1. Step 1: which T7 rows a fixture could fake (attribution, priors) and
   which a key can pin (T7.3 realised values and T7.4 variances against the
   T1 document; T7.1's decision date and posture against the memo).
2. Steps 2–3: a CP-8 fixture in `tests/canonical_route_fixtures.py` from
   `load_contract`'s eight registers, quoting whole tokens of a small
   decision memo and an outcome extract added to the pack; the five contract
   tests; CP-8 joins `ADAPTER_MODULES`.
3. Step 4: `tests/test_lite_decision_ledger_route.py` — completes, proves,
   freezes; ceiling; a blocked CP-0 verdict on CP-8 calls nothing; the pair
   joins `ADAPTER_ROUTES`; the guard updated.
4. Step 5: `qualification/vmo2-fy2025-decision-ledger/` — the T0 decision
   memo (owner-authored, dated between Q3 and Q4, stating a posture, a
   thesis and expected metrics for Q4) admitted as a document with its
   provenance stated as owner-authored; the Q3 release as T0 context; the Q4
   release as T1.
5. Step 6: keys — `expects_ready: ["CP-8"]`; register keys on T7.3 (the
   realised Q4 figures the memo's metrics name, from the Q4 release) and
   T7.4 (the variance direction for each); T7.1 `Decision Date` from the
   memo; a citation key on the Q4 line that carries the realised figure.
6. Step 7 when authorized.

## RED-first acceptance checks

The contract and route tests fail at the base (`HANDOFF_MODULE_UNSUPPORTED`)
and pass after; `assert_measurable` accepts the set; the memo's digest is
recorded in `RESULT.md` with its authorship.

## Concern boundaries and gates

Three concerns: CP-8 fixture and contract tests; route test and enablement;
the set. Serial backend gate; no provider call except the authorized step 7.
