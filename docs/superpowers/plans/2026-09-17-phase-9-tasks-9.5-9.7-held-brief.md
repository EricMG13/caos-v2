# Completion Phase 9 Tasks 9.5–9.7 — the three LITE pathways held on the vendor

- **Base inspected:** `e59ad7b`. Held; no branch until the dependency lands.
- **Dependency:** `docs/requests/2026-09-17-lite-producers.md` (Task 8.5)
  answered by an upstream build shipping CP-L20, CP-L23, CP-L30 and CP-L40,
  or by an authorized edit carrying the objects on the catalog edges.
- **Fixes when released:** `docs/COMPLETION_PLAN.md` O03; O01 for CP-3C,
  CP-2H, CP-4C and CP-1A; O02 for three pathways.

## Why held

`CP_DEPLOY_V_EXECUTION_PROFILES_v1.json` `retained_lite_capabilities`:

| Consumer | Accepts | Reaches it on an edge? |
|---|---|---|
| CP-2A | `lite_fundamental_credit_screen`, `lite_liquidity_sensitivity_screen` | **no** |
| CP-2H | the same two | **yes** -- `lite_liquidity_sensitivity_screen`, catalog line 2484 |
| CP-3C | `lite_liquidity_sensitivity_screen`, `lite_market_recovery_opportunity_screen`, `lite_legal_structure_capacity_screen` | **no** |
| CP-4C | `lite_legal_structure_capacity_screen` | **yes** -- catalog line 2491 |

Corrected against the bundle by Task 8.5, because the first version of this
brief was wrong in three ways. CP-L10 names one `owned_object`
(`lite_financial_change_screen`), but the producers of the other four are not
missing: `superseded_module_ids` absorbs CP-L20, CP-L23, CP-L30 and CP-L40 into
CP-L10, whose `SKILL.md` carries each as an absorbed phase with its own owned
object. Two of the four objects already reach a consumer on a catalog edge. And
`invocation.named_objects` (§46.1) does not hold a consumer forever -- it drops a
boundary no route input can meet. What actually holds these routes is
`ADAPTER_ROUTES` plus the ledger's policy that a vendor owner must exist before a
route is enabled, so they are refused `HANDOFF_MODULE_UNSUPPORTED` before any
attempt today. CP-2A and CP-3C are the two consumers whose objects are carried
nowhere, and they are what the request asks for.

| Pathway | Nodes | Unproven modules once released | Corpus demand |
|---|---|---|---|
| `LITE_COVENANT_REFINANCING` | CP-0, CP-L10, CP-3C, CP-5 | CP-3C | debt documents, maturity schedule |
| `LITE_DISTRESSED_RESTRUCTURING` | CP-0, CP-L10, CP-2A, CP-2H, CP-4C | CP-2H, CP-4C | ratings evidence; a distress gate (CP-4C `block_restructuring_scenario_without_documented_distress_gate_or_resolved_scope`) |
| `LITE_FULL_CREDIT_SCREEN` | CP-0, CP-L10, CP-1A, CP-1C, CP-2A, CP-2H, CP-3C, CP-4C, CP-5 | CP-1A, CP-2H, CP-3C, CP-4C | all of the above plus the business fact pack's sources |

## What may be done now

- The four modules' FULL fixtures and contract tests may be written under
  Phase 11's tasks (11.6, 11.8) where they are needed anyway; their LITE
  identity variants are written here and marked
  `pytest.mark.skip(reason="docs/requests/2026-09-17-lite-producers.md")`
  with the request as the reason, never silently.
- No route is enabled; `tests/test_disabled_routes.py` keeps proving these
  three refuse before any attempt.

## When released

Each task runs the seven-step template as Tasks 9.2 and 9.3 do: step 1 on
the new producers' registers as well as the consumers'; the producer
modules join `ADAPTER_MODULES` with their own contract tests; the route
tests prove the named-object release on each consumer; the corpus rows in
`qualification/DOCUMENTS.md` are sourced; keys authored; the authorized run.
