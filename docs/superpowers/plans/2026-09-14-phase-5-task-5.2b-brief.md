# Phase 5 Task 5.2b — accepted CP-CF host extension

Base: `859f548`, isolated branch `codex/phase5-5-2b`. Astra high implementation;
ordinary exact-range review at this slice. Confidence review then adversarial
review are coordinator-owned at whole-phase completion, both actual xhigh.
No rewrite tournament or provider calls. Tests use UUID databases on 55437.

## Scope and binding choices

Own `server/engine/route.py`, `server/methodology/{bundle,host,host_pin,forecast,
handoff,invocation,canonical}.py`, `server/store/gates.py`, generated host
manifest/skill, `scripts/host_manifest.py`, and `tests/test_forecast_*.py`.
No vendor, deliverable, API or UI edits. Docker coordinator must add
`COPY methodology/ ./methodology/` beside the server/vendor copies.

Refine decision §17's identity pair without changing legacy vendor build IDs:
the route's immutable `host_manifest_sha256` predicate pins the generated host
manifest alongside its existing vendor build/manifest pins. Changed host
manifest, skill or calculator bytes refuse before execution and acceptance.
Generate with `python scripts/host_manifest.py`; never edit the vendor manifest.
The only executable calculator is the imported host `cash_flow_forecast`.

Append CP-CF at stage 100 only to RELATIVE_VALUE for execution, with REQUIRED
CP-0/CP-1/CP-2G/CP-4 edges. Vendor T8 retains exactly vendor modules: its grammar
cannot name the stage-100 extension. Host envelope construction validates the
shared fields through the vendor's stage-99 grammar then binds the actual
stage-100 occurrence and attempt digest. No vendor grammar changes.

CP-CF's canonical Analysis contains one bounded `caos-forecast-v1` JSON block
with exactly request/bindings/forecast. Host recomputation must equal the
complete result. Every input leaf (including empty repayment arrays) needs an
exact assignment quoted in both the accepted owner and its anchored citations.
Opening/period/unit/perimeter owner is CP-1; drivers/tolerance CP-2G;
contractual owner CP-4. Read identity and entire accepted lineage through the
existing canonical executor before checking these data bindings. Readers use
`accepted_handoff` then `forecast_projection(markdown)`; the latter alone
does not establish provenance.

CP-2G's `cp2g.cp_model_forecast_drivers` table maps acquisitions_disposals and
dividends_paid to acquisitions/distributions at exact case/period/year and
CURRENCY_MM. Nonzero net_equity_issue_repay or other_investing_financing refuses;
division growth never becomes revenue. Other movements need explicit accepted
supplemental assignments. This bounded contract requires a source-supplied
assignment dataset; it does not infer missing operational forecasts from the
small annual-report extract or claim economic/live qualification.

## REDs and verification

Captured failing tests for missing host authority/pin, modified host bytes,
missing projection parser, altered/incomplete forecast, arbitrary calculator
selection, absent bindings, duplicate JSON keys, and real extended route
execution. Focused suite includes a real ten-node accepted route with exact
cash 145.000000, and refuses missing/wrong-owner bindings and changed output.

Run focused forecast/route/canonical tests, Ruff, mypy, vocabulary/tested gates,
and refresh GitNexus. Coordinator reruns integrated complete gates and records
task/phase acceptance. No UI exposure is certified by this slice alone.
