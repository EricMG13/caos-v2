# Completion Phase 9 Task 9.2 — `LITE_RELATIVE_VALUE` (CP-0 → CP-L10 → CP-1C)

- **Base inspected:** `e59ad7b`; rebase onto Phase 8's accepted commit.
  Isolated branch `codex/phase9-9-2`; UUID-owned test database on 55437.
- **Dependencies:** Task 8.1; a peer table document from Task 8.4's sourcing
  list; remediation T7 before the live run.
- **Fixes:** `docs/COMPLETION_PLAN.md` O02 (one pathway); exercises the
  named-object release (§46.1) on a second route.
- **Executor (template rows):** step 1 Opus 5 `xhigh` `ultrathink` on CP-1C's
  registers under a LITE upstream; steps 2–4 Opus 5 `medium`; step 5 Opus 5
  `low`; step 6 Opus 5 `medium`; step 7 the coordinator under authorization.

## Findings at the inspected base

- Catalog: nodes CP-0, CP-L10, CP-1C; edges CP-0 → CP-L10, CP-0 → CP-1C,
  CP-L10 → CP-1C, all REQUIRED; `SCREENING_ONLY`.
- CP-1C is proven under FULL (`tests/test_owner_contracts.py`, fixture in
  `tests/canonical_route_fixtures.py`); its LITE block
  (`vendor/deploy-v/skills/cp-1c-peer-benchmark/SKILL.md:44-50`): stays a FULL
  run under `LITE_CREDIT_22`, `NAMED_LITE_OBJECT_ACCEPTED`,
  `accepted_lite_object_ids: lite_financial_change_screen`, `allowed_use:
  SCREENING_ONLY`, `missing_input_behavior: UPGRADE`. CP-L10 owns that object
  (catalog `artifact_contract.owned_object`), so `invocation.named_objects`
  releases CP-1C once CP-L10 is accepted — the same mechanism that holds CP-5
  on the earnings route (`tests/test_handoff_invocation.py`,
  `test_cp5_is_not_invoked_without_an_accepted_named_lite_object`).
- The route's prompt for CP-1C carries CP-0's and CP-L10's records as
  upstream with `allowed_use` `SCREENING_ONLY` from the catalog edge.

## Contract and scope

1. Step 1: which CP-1C benchmark registers a fixture could fake against a
   LITE upstream (peer selection, basis, derived ratios) and which cells a
   key must pin; the vendor's `required_payload_fields` for LITE are
   unenforced (ledger) — the host adds no rule.
2. Steps 2–3: a CP-1C fixture under a LITE `HostIdentity` (profile
   `LITE_CREDIT_22`, upstream refs CP-0 and CP-L10) in
   `tests/canonical_route_fixtures.py`, quoting whole tokens of a peer table
   extract added to the pack; tests
   `test_cp1c_under_lite_accepts_the_named_object_and_keeps_screening_scope`,
   `test_cp1c_under_lite_is_held_until_cp_l10_is_accepted`, and the five
   contract tests parametrised for the LITE identity.
3. Step 4: `tests/test_lite_relative_value_route.py` — completes, proves,
   freezes; ceiling; a blocked CP-L10 holds CP-1C; a restricted CP-L10 keeps
   its limitation in CP-1C's prompt and record; `ADAPTER_ROUTES` gains the
   pair; the enabled-set guard updated.
4. Step 5: a peer table document (sector peers' FY2025 revenue, EBITDA, debt,
   cash with provenance) sourced by the owner, admitted into
   `qualification/vmo2-fy2025-relative-value/` beside the two releases (or
   `ccl-fy2025-relative-value/` with cruise peers).
5. Step 6: keys — `expects_ready: ["CP-L10", "CP-1C"]`; projections
   `decision_scope SCREENING_ONLY` for CP-L10 and CP-1C; register keys on
   CP-1C's peer-comparison register (the issuer's and one peer's leverage
   from the documents, `[Calculated]` where derived and labelled so) and on
   `TL10.2`; citations naming the peer table's fact lines.
6. Step 7 when authorized.

## RED-first acceptance checks

The named tests fail at the base (CP-1C refuses under a LITE identity;
route refuses before any attempt) and pass after; `tests/test_owner_contracts.py`
FULL cases unchanged; `tests/test_handoff_invocation.py` named-object tests
green.

## Concern boundaries and gates

Three concerns: the CP-1C LITE fixture and contract tests; the route test and
enablement (constant line only; rebase onto the remediation wave-3
integration if needed); the set. Serial backend gate; no provider call except
the authorized step 7.
